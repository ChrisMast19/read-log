[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client routing / refreshes resolve to the app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
